name: alpha_lib
description: A database-less library for astronomical calculations.
version: 1.0.7
repository: https://github.com/pat-mart/alpha_lib

environment:
  sdk: ">=3.2.0 <3.3.1"

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - astronomy

# Add regular dependencies here.
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
